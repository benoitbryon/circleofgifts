# Project's bootstrap configuration file.
# 
# The goal of "bootstrap" is to install necessary tools to continue deployment,
# i.e. necessary tools to generate project's configuration.

[buildout]
extends =
    base.cfg
parts =
    bootstrap

[bootstrap]
recipe = zc.recipe.egg
eggs =
    PasteScript
